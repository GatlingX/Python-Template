# Instructions for Ralph

When creating or updating `prd.json`, the final item in the list of requirements must always include the following tasks:

1. Update the `README.md` if necessary to reflect changes in the project.
2. Update the `Makefile` to include any new commands or build steps required.
3. Document how to execute and test the implemented code.
